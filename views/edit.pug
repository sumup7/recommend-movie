extends layout

block content
  h3 映画の感想の編集
  form(method="post", action=`/movies/${movie.movieId}?edit=1`)
    div
      h5 映画のタイトル(255文字まで)
      input(type="text" name="movieTitle" maxlength="255")
    div
      h5 映画の関連情報
      textarea(name="movieDetails" maxlength="255")
    div
      h5 ネタバレなし感想 (ネタバレ禁止です)
      textarea(name="movieReview" maxlength="1000")
    div
      h5 ネタバレあり感想 (ネタバレありです)
      textarea(name="movieReviewAll" maxlength="1000")
    button(type="submit") 映画の感想を編集する